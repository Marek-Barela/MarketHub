import React, { FC } from 'react';
import { PageWrapper, StockList } from '../../components';

const Homepage: FC = () => {
	return (
		<PageWrapper>
			<StockList />
		</PageWrapper>
	);
};

export default Homepage;

import React, { FC } from 'react';
import { PageWrapper } from '../../components';

const Homepage: FC = () => {
	return (
		<PageWrapper>
			<h1>hello</h1>
		</PageWrapper>
	);
};

export default Homepage;

import React from 'react';
import { PageWrapper, GraphPreview } from '../../components';

const Overview = () => {
	return (
		<PageWrapper>
			<GraphPreview />
		</PageWrapper>
	);
};

export default Overview;
